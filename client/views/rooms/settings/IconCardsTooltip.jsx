import React from 'react';

import styled from "@material-ui/core/styles/styled";

import IconHelp from '@material-ui/icons/HelpOutline';

export default styled(IconHelp)({
  '&:hover': {
    cursor: 'help'
  }
});